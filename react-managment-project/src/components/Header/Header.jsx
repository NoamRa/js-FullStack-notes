import React from 'react';

const head = (props) => {
    return (
        <header>
            <p>{props.title}</p>
            <button>buttonnn</button>
        </header>
    );
}

export default head;
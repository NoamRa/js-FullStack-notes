import React from 'react';

const ProductInfo = (props) => {
    render(){

        return (
            <div>
                ProductInfo
            </div>
            );
    }
}

export default Header;